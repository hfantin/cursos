package com.github.hfantin.entidades

import io.quarkus.test.junit.QuarkusTest

@QuarkusTest
class UsuarioTest {
/*
    // Testes de active records - import io.quarkus:quarkus-panache.mock
    @Test
    fun testarSeFindByIdOptionalRetornaUsuarioCorreto() {
        val u = Usuario()
        val usuario = Optional.of<PanacheEntity>(u)
        Mockito.when(usuario.findByIdOptional(40).thenReturn(usuario))
        Assertions.assertSame(u, usuario.findByIdOptional(40).thenReturn(usuario))
    }
*/



}