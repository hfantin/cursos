# mudulo que faz a multiplicacao de a por b

defmodule Multiplicador do
    def  multiplicar(a, b )do
        a * b        
    end

    def multiplique(a, b \\2), do: soma(10, a * b)

    defp soma(a, b), do: a + b
end